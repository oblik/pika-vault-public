###############################################
# GLOBAL / SECRETS
###############################################
# Deployer private key (TEST-ONLY; never commit real keys)
DEPLOYER_PK=0xYOUR_TEST_PRIVATE_KEY

###############################################
# RPC ENDPOINTS (used via CLI flags or scripts)
###############################################
ARB_SEPOLIA_RPC=https://arb-sepolia.g.alchemy.com/v2/...
BASE_SEPOLIA_RPC=https://base-sepolia.g.alchemy.com/v2/...

###############################################
# CHAINLINK CCIP (Sepolia testnets)
# Pull latest from Chainlink CCIP Directory if they change
###############################################
# CCIP Routers (per network)
CCIP_ROUTER_ARB_SEPOLIA=0xArbCcipRouterAddress
CCIP_ROUTER_BASE_SEPOLIA=0xBaseCcipRouterAddress

# CCIP Chain Selectors (uint64)
# Base Sepolia (example): 10344971235874465080
CCIP_BASE_SEPOLIA_SELECTOR=10344971235874465080

###############################################
# CIRCLE CCTP v2 (USDC burn/mint)
# Get these from Circle’s official v2 address directory per network
###############################################
# On Arbitrum Sepolia (source)
CCTP_TOKEN_MESSENGER_V2_ARB_SEPOLIA=0xTokenMessengerV2Address
USDC_ARB_SEPOLIA=0xUSDCAddressOnArbSepolia

# Destination Circle Domain IDs (uint32, NOT chainId)
# Example: Base = 6  (confirm in docs)
CCTP_DEST_DOMAIN_BASE=6

# Finality & fees
# 2000 = Standard finality (no fast fee). 1000 = Fast finality (set max fee).
CCTP_MIN_FINALITY=2000
CCTP_MAX_FEE=0

# Where USDC should mint on Base (destination treasury)
DEST_TREASURY_BASE=0xYourBaseTreasuryAddress

###############################################
# CONTRACTS PRODUCED BY OUR SCRIPTS
###############################################
# After running 01_DeployBaseSepolia_NavReceiver.s.sol
NAV_RECEIVER_BASE=0xDeployedNavReceiverOnBase

# After running 02_DeployArbSepolia_Core.s.sol
NAV_PUSHER_ARB=0xDeployedNavPusherOnArb
CCTP_BRIDGER_ARB=0xDeployedCctpBridgerOnArb

###############################################
# DEMO AMOUNTS
###############################################
# USDC has 6 decimals (e.g., 10 USDC = 10000000)
AMOUNT_USDC_6DP=10000000

# NAV with 18 decimals (e.g., 1.005e18 = 1005000000000000000)
TEST_NAV_E18=1005000000000000000

###############################################
# LAYERZERO (OVault async-redeem OApps) — optional here,
# but add now so they’re ready when wiring OApps
###############################################
# LZ Endpoint addresses (per network)
LZ_ENDPOINT_ARB_SEPOLIA=0xLzEndpointArbSepolia
LZ_ENDPOINT_BASE_SEPOLIA=0xLzEndpointBaseSepolia

# LZ Endpoint IDs (EIDs) for networks (uint32)
LZ_EID_ARB_SEPOLIA=40231
LZ_EID_BASE_SEPOLIA=40245

# Deployed OApps (fill after you deploy them)
ASYNC_COMPOSER_HUB_BASE=0xAsyncComposerOnBase
SPOKE_REDEEM_OAPP_ARB=0xSpokeRedeemOnArb

###############################################
# CIRCLE PAYMASTER (apps/wallet sample) — optional
###############################################
# Circle Paymaster addresses (v0.7/v0.8 differ per chain; use docs)
PAYMASTER_V08_ADDRESS=0xCirclePaymasterV08
USDC_ADDRESS=$USDC_ARB_SEPOLIA
# Owner key for the AA demo (test wallet; can be same as DEPLOYER_PK for sandbox)
OWNER_PRIVATE_KEY=0xYOUR_TEST_PRIVATE_KEY
BUNDLER_RPC=https://your-bundler-or-gas-station
ENTRYPOINT_ADDRESS=0xEntryPointAddress
